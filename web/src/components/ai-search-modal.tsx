"use client";
import { MessageCircle, X } from "lucide-react";
import { useEffect, useState } from "react";
import { Concierge } from "@/components/concierge";
import { Button } from "@/components/ui/button";

export function AiSearchButton({light=false}:{light?:boolean}){const[open,setOpen]=useState(false);useEffect(()=>{function close(e:KeyboardEvent){if(e.key==="Escape")setOpen(false)}window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[]);return <><Button onClick={()=>setOpen(true)} variant={light?"outline":"default"} className={light?"border-white/25 bg-white/10 text-white hover:bg-white hover:text-[#111111]":"bg-black px-4 sm:px-6 text-white shadow-[0_12px_35px_rgba(0,0,0,.22)] hover:bg-[#222]"}><MessageCircle className="size-4 sm:mr-2"/><span className="hidden sm:inline">Ask Noria</span></Button>{open&&<div className="fixed inset-0 z-[80] flex items-end justify-center bg-[#07142b]/55 p-3 backdrop-blur-md sm:items-center sm:p-6" onMouseDown={(e)=>{if(e.target===e.currentTarget)setOpen(false)}}><div role="dialog" aria-modal="true" aria-label="Ask Noria" className="w-full max-w-2xl rounded-[32px] bg-white p-3 shadow-[0_40px_120px_rgba(0,0,0,.35)]"><div className="flex items-center justify-between px-4 pb-2 pt-2"><span className="text-sm font-semibold text-black/70">Ask Noria</span><button onClick={()=>setOpen(false)} className="flex size-10 items-center justify-center rounded-full bg-black/5 text-black transition hover:bg-black hover:text-white" aria-label="Close"><X className="size-4"/></button></div><Concierge/></div></div>}</>}
